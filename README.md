1:Create virtual Enviroment
    - python -m venv .venv : to create 
    - .venv\Scripts\Activate.ps1

2. Install required packages 
    - pip install fastapi "uvicorn[standard]" : to install uvicorn 
    - pip install sqlalchemy psycopg2-binary : to install database package
